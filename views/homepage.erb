<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Homepage</title>
    <link rel="stylesheet" href="/css/styles.css">
  </head>

  <body>
    <header>
      <h1>&#127968; MakersBnB &#127968;</h1>
    </header>

      <div>
        <div> 
          <% flash.keys.each do |type| %>
            <div data-alert class="flash <%= type %> alert-box radius">
                <%= flash[type] %>
                <a href="/homepage" class="close">&times;</a>
            </div>
          <% end %>
        </div>

        <div> <!-- button navigation -->
          <p class="homepage_nav_buttons">
            <form action="/session/destroy" method='post'>
              <button type="submit"> Logout </button>
            </form>
            <form action="/property/new" method="get">
              <button type="submit"> List a property </button>
            </form>
          </p>
        </div>

        <div>
          <h2 class="property_listing_heading"> Here are the available listings: </h2>
            <% @properties.each do |property| %>
               <p class="property_listing">
                  <%= property.title%>
                  <br>
                  <%= "Property area: #{property.postcode}"%>
                  <br>
                  <%= "Property details #{property.description}"%>
                  <form action="/property/<%= property.id %>" method="get" class="view_property_button">
                  <button type="submit"> View Property </button>
                  </form>
                </p>
              <% end %>
        </div>
      
        <div>
          <h2 class="property_listing_heading">&#128231; Messages &#128231;</h2>
            <% if @request_messages %>
              <% @request_messages.each do |message| %>
                <p class="request_message">
                <%= "New Request: Someone has requested #{message.title} "%>
                  <form action="/property/<%= message.property_id %>/confirm" method="post" class="confirm_request_button">
                  <input type="hidden" name="message_id" value='<%=message.id%>'>
                  <input type="hidden" name="sender_id" value='<%=message.sender_id%>'>
                  <button type="submit"> Confirm request </button>
                  </form>
                </p>
              <% end%>
            <% end%>
            <% if @confirmed_messages%>
            <% @confirmed_messages.each do |message| %>
              <p class="approved_message">
              <%= "Your request has been approved" %> <%# make message.confirmed message a join method and input name of property into confirmation%>
                  <form action="/property/<%= message.property_id %>/accept_confirmation" method="post" class="clear_button">
                  <input type="hidden" name="message_id" value='<%=message.id%>'>
                  <button type="submit"> Clear message and view property </button>
                  </form>
                </p>
              <% end%>
            <% end%>
        </div>
      </div>
  <footer>
     🦍 © In the AirBnB Tonight 🦍 | 
    <a href="https://github.com/MattDawson2020/in_the_airbnb_tonight">Project GitHub</a>
  </footer>
  </body>
</html>
